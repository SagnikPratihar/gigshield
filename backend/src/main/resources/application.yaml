spring:
  application:
    name: backend

  datasource:
    url: jdbc:sqlite:../database/gigshield.db
    driver-class-name: org.sqlite.JDBC

  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true
